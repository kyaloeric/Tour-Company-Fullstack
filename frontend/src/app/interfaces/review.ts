export interface Review {
    reviewID?: string; 
    reviewComment: string;
    userID: string;
    tourID: string;
}
  
export interface ReviewDetails {
    reviewID?: string; 
    reviewComment: string;
    userID: string;
    tourID: string;
  }
  